@Library('my_library') _

pipeline{
    agent any
       stages{
        stage('chek'){
            steps{
                script{
                   def config = [
                      branch: 'main',
                      credentialsId: 'gith_hub',
                      url: 'https://github.com/Akash923-ops/Jenkins-programs.git'
                ]
                gitCheckout(config)
            }
        }
       }
}}
