@Library('my-library') _

pipeline{
    agent any
       stages{
        stage('chek'){
            steps{
                checkout scmGit(branches: [[name: '*/main']], 
                extensions: [], 
                userRemoteConfigs: [[credentialsId: 'gith_hub', url: 'https://github.com/Akash923-ops/Jenkins-programs.git']])
            }
        }
       }
}